const logger = require('../../Services/Logger/Logger');

const logger = (req, res, next) => {
    // get url
    // match
    next();
};

module.exports = isAdmin;

// who logs in
// who signs in when blocked
// who creates a record
// who updates a record
// 